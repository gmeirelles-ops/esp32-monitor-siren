## Context

Capability vivia em `openspec/specs/diatom-laser-marking/`. Código: `DiatuLaserTcpServer`, docs laser-reference.

## Goals / Non-Goals

**Goals:** nome único **Diatu** na capability e links ativos.  
**Non-Goals:** reescrever archives; alterar strings “Diaotu” do menu do equipamento (marca do hardware/software de controle).

## Decisions

1. Rename pasta + título da spec.
2. Substituir `diatom-laser-marking` → `diatu-laser-marking` em docs ativos.
3. Não tocar `openspec/changes/archive/**`.

## Risks

Baixo — só documentação.
