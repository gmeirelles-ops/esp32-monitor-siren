## ADDED Requirements

### Requirement: Capability retired
A capability `dev-label-file-export` (download ZPL em debug) SHALL ser marcada **RETIRED**. O produto interno não mantém pipeline ZPL.

#### Scenario: Spec principal
- **WHEN** um desenvolvedor abre `openspec/specs/dev-label-file-export/spec.md`
- **THEN** encontra Purpose RETIRED apontando para `diatu-laser-marking`
