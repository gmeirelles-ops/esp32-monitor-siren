## REMOVED Requirements

Capability `nicelabel-reference-workflow` deixa de ser requisito ativo do produto. Referência histórica em `docs/label-reference/` pode permanecer marcada como obsoleta.

Razão: laser-only; NiceLabel/ZPL fora do fluxo.
