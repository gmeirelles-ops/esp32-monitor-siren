## Why

Cerca de 22 specs em `openspec/specs/` ainda têm `Purpose: TBD` após arquivamento das changes. Isso prejudica o CLI OpenSpec (`openspec instructions`) e dificulta novas propostas — o contexto de cada capability fica implícito nos requirements apenas.

## What Changes

- Preencher seção `## Purpose` em todas as specs com `TBD`.
- Revisão rápida de consistência (nome da capability alinhado ao conteúdo).
- Nenhuma alteração de requirements — apenas documentação de propósito.

## Capabilities

### New Capabilities

_(nenhuma)_

### Modified Capabilities

- Todas as specs com `Purpose: TBD` (lista em `tasks.md`)

## Impact

- **Somente** arquivos `openspec/specs/**/spec.md`
- Sem mudança de código de produção
