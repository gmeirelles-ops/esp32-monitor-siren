## 1. Formatação e queries

- [x] 1.1 Completar `production_report_export.dart`: BOM UTF-8, escaping, CSV detalhado de testes, decimal PT-BR
- [x] 1.2 Query Drift dos testes no período/filtros do dashboard (reutilizar ou adicionar método)
- [x] 1.3 Testes unitários de CSV (resumo + detalhe + BOM)

## 2. UI export

- [x] 2.1 Incluir CSV em `ReportExportFormat` / `pickReportExportOptions` (resumo vs testes)
- [x] 2.2 Wire no `DashboardScreen` com `getSaveLocation` + escrita do arquivo

## 3. Docs (leve)

- [x] 3.1 Mencionar export CSV do Painel em `docs/PRODUCAO.md` (gestor)

## 4. Verificação

- [x] 4.1 `flutter test` verde
