/// Breakpoint desktop (NavigationRail + formulários em coluna limitada).
const double kDesktopBreakpoint = 900;

/// Largura máxima de formulários em desktop (Lote, campos de Configurações).
const double kFormMaxWidth = 600;

/// Largura máxima de páginas operacionais (Gravação, listas médias).
const double kPageContentMaxWidth = 900;
